--------------------------
Running benchmark softmax_part2 in suite llama
===== Starting iteration 0 =====
running with gpt
```python
def invariant(i: int, input: List[int], max_pos: int, max_val: int, output: List[int]) -> bool:
    return i >= 0 and i <= max_pos and output == vec_map(
        vec_scalar_sub(max_val, vec_slice(input, 0, i)), lambda x: integer_exp(x)
    )
```

def invariant(i: int, input: List[int], max_pos: int, max_val: int, output: List[int]) -> bool:
    return i >= 0 and i <= max_pos and output == vec_map(
        vec_scalar_sub(max_val, vec_slice(input, 0, i)), lambda x: integer_exp(x)
    )

Passing solution to the parser
Parser solution passed the parser
===== Starting iteration 1 =====
running with gpt
