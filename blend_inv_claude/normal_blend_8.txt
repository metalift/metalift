--------------------------
Running benchmark normal_blend_8 in suite blend
===== Starting iteration 0 =====
running with claude

def invariant(active: List[int], base: List[int], i: int, opacity: int, out: List[int]) -> bool:
    return i >= 0 and i <= len(base) and out == vec_elemwise_add(
        vec_scalar_mul(opacity, vec_slice(active, 0, i)),
        vec_scalar_mul(32 - opacity, vec_slice(base, 0, i))
    )

Passing solution to the parser
Parser solution passed the parser
===== Starting iteration 1 =====
running with claude
