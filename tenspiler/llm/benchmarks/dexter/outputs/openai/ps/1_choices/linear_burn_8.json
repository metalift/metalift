[
    "The given C++ function `linear_burn_8` is performing element-wise addition on two 2D vectors, `base` and `active`, and then subtracting 255 from each element. \n\nThis can be performed in Python using the provided functions in the following way:\n\n```python\ndef linear_burn_8(base, active):\n    # First, use matrix_elemwise_add to add base and active element-wisely\n    result = matrix_elemwise_add(base, active)\n\n    # Then, use matrix_scalar_sub to subtract 255 from each element\n    result = matrix_scalar_sub(255, result)\n\n    return result\n```\n\nThis code first uses the function `matrix_elemwise_add` to add the `base` and `active` matrices together on an element-wise basis, producing a new matrix. It then calls `matrix_scalar_sub` to subtract 255 from each element in the resultant matrix and returns this final matrix."
]
