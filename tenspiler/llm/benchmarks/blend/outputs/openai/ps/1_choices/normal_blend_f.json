[
    "Here is your requested C++ function translated into Python, using the provided set of functions:\n\n```Python\ndef normal_blend_f(base, active, opacity):\n    # Multiply 'active' vector by 'opacity'\n    intermediate_result1 = vec_scalar_mul(opacity, active)\n\n    # Multiply 'base' vector by  (1 - 'opacity')\n    intermediate_result2 = vec_scalar_mul(1 - opacity, base)\n\n    # Sum the two intermediate results to get the output vector\n    out = vec_elemwise_add(intermediate_result1, intermediate_result2)\n\n    return out\n```"
]
